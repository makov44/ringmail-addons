<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="registration.list">
      <field name="name">registration list</field>
      <field name="model">registration.registration</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="registration.action_window">
      <field name="name">registration window</field>
      <field name="res_model">registration.registration</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="registration.action_server">
      <field name="name">registration server</field>
      <field name="model_id" ref="model_registration_registration"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="registration" id="registration.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="registration.menu_1" parent="registration.menu_root"/>
    <menuitem name="Menu 2" id="registration.menu_2" parent="registration.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="registration.menu_1_list" parent="registration.menu_1"
              action="registration.action_window"/>
    <menuitem name="Server to list" id="registration" parent="registration.menu_2"
              action="registration.action_server"/>
    -->
  </data>
</odoo>